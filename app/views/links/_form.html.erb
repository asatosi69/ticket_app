<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type_id %>
    <%= form.number_field :type_id, id: :link_type_id %>
  </div>

  <div class="field">
    <%= form.label :stage_id %>
    <%= form.number_field :stage_id, id: :link_stage_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
